import React from 'react';

import { Logo } from '../Logo/Logo';
import { FooterContainer } from './FooterStyles';

export const Footer = () => {
  return (
    <FooterContainer>
      <Logo />
    </FooterContainer>
  );
};
