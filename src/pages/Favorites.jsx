import React from 'react';

import { Header } from './PageStyles';

const Favorites = () => {
  return (
    <section>
      <Header>Favorites</Header>
    </section>
  );
};

export default Favorites;
